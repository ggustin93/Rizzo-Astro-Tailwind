---
import { getEntry, getDataEntryById } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { Calendar, Mail, Phone } from 'astro-feather-icons';
import IconWhatsapp from '../../components/IconWhatsApp.astro';
import IconLinkedin from '../../components/IconLinkedIn.astro';
import IconGoogleMaps from '../../components/IconGmaps.astro';
import ctaIllustration from '../../assets/images/illustration_cta_2.svg';

export const prerender = true;

export async function getStaticPaths() {
  return ['fr', 'en', 'it'].map(lang => ({ params: { lang } }));
}

const { lang } = Astro.params;
const contactEntry = await getDataEntryById('contact', 'contact');
if (!contactEntry) {
  throw new Error("Contact entry not found! Make sure you have a `contact.yml` file in `src/content/contact`.");
}
const contactData = contactEntry.data[lang];

// Récupérer la configuration du site
const siteConfigEntry = await getEntry('config', 'site-config');
if (!siteConfigEntry) {
    throw new Error("Site config entry not found in contact page! Make sure you have a `site-config.md` file in `src/content/config`.");
}
const calendarLink = siteConfigEntry?.data?.calendarLink || 'https://cal.com/c.rizzo-avocat.be/rendez-vous?duration=15';
const globalContactInfo = siteConfigEntry?.data?.contactInfo;

if (!globalContactInfo) {
  throw new Error("FATAL: Global contact info not found in site-config.yml for Contact page.");
}

const contactContent = (await getEntry('contact', 'contact'))?.data?.[lang] || {};

const iconComponents = {
  calendar: Calendar,
  mail: Mail,
  phone: Phone,
  'message-circle': IconWhatsapp,
  linkedin: IconLinkedin,
  'map-pin': IconGoogleMaps
};

interface ContactButton {
  type: string;
  icon: keyof typeof iconComponents;
  text: string;
  subtext: string;
  action?: string;
}

const getAction = (button: ContactButton) => {
  if (!button.type || !button.action) return button.action || button.subtext;

  switch(button.type) {
    case 'email':
      return `mailto:${globalContactInfo.email}`;
    case 'phone':
      return `tel:${globalContactInfo.phone}`;
    case 'whatsapp':
      return `https://wa.me/${globalContactInfo.whatsapp}`;
    case 'linkedin':
      return globalContactInfo.linkedin;
    case 'maps':
      return `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(globalContactInfo.address)}`;
    default:
      return button.action;
  }
};

const getDisplayText = (button: ContactButton) => {
  switch(button.type) {
    case 'email':
      return globalContactInfo.email;
    case 'phone':
      return globalContactInfo.phone;
    case 'maps':
      return globalContactInfo.address;
    default:
      return button.subtext;
  }
};

const seoData = {
  title: contactData.seo?.title || `Contact - Christine Rizzo`,
  description: contactData.seo?.description || "Prenez contact avec Christine Rizzo, avocate spécialisée en droit du travail à Bruxelles.",
  image: contactData.seo?.image || 'https://v5--crizzo.netlify.app/_astro/logo_crizzo_calli3.8EyI_Q3S_2joHla.svg',
  keywords: contactData.seo?.keywords?.join(', ') || ''
};
---

<BaseLayout 
  title={seoData.title}
  description={seoData.description}
  image={seoData.image}
  keywords={seoData.keywords}
  lang={lang}
>
  <main class="container mx-auto px-4 py-8 md:py-16">
    <h1 class="text-3xl md:text-4xl font-light mb-8 md:mb-12 text-black text-center">{contactData.heading}</h1>
    
    <div class="full-bleed bg-employeur py-10 md:py-16">
      <div class="container mx-auto px-3 md:px-4">
        <div class="flex flex-col lg:flex-row gap-8 items-center lg:items-start">
          <!-- Image visible seulement sur mobile, au-dessus des boutons -->
          <div class="w-full lg:hidden mb-6">
            <img
              src={ctaIllustration.src}
              alt="illustration contact"
              class="w-full h-auto max-w-[200px] mx-auto"
              width={200}
              height={200}
              loading="lazy"
            />
          </div>
          
          <div class="w-full lg:w-2/3 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4 sm:gap-6 md:gap-8">
            {contactData.buttons.map((button: ContactButton) => {
              const IconComponent = iconComponents[button.icon];
              
              return (
                <div class="flex flex-col items-center">
                  <button
                    onclick={getAction(button)}
                    class="w-16 h-16 md:w-20 md:h-20 bg-white rounded-full flex items-center justify-center mb-3 md:mb-5 hover:bg-travailleur transition-all duration-300 transform hover:scale-105 group shadow-md cursor-pointer"
                    data-cal-link={button.type === 'appointment' ? calendarLink.replace('https://cal.com/', '') : undefined}
                    data-cal-config={button.type === 'appointment' ? '{"layout":"month_view"}' : undefined}
                  >
                    <IconComponent class="w-7 h-7 md:w-9 md:h-9 text-employeur group-hover:text-white transition-colors duration-300 pointer-events-none" />
                  </button>
                  <span class="text-sm md:text-base uppercase text-white font-semibold leading-relaxed tracking-wide mb-1 md:mb-2 px-2 text-center">{button.text}</span>
                  <button
                    onclick={getAction(button)}
                    class="text-sm md:text-base text-gray-300 font-normal italic text-center hover:text-white transition-colors duration-300 hover:underline cursor-pointer"
                    data-cal-link={button.type === 'appointment' ? calendarLink.replace('https://cal.com/', '') : undefined}
                    data-cal-config={button.type === 'appointment' ? '{"layout":"month_view"}' : undefined}
                  >
                    {getDisplayText(button)}
                  </button>
                </div>
              );
            })}
          </div>
          
          <!-- Image visible seulement sur desktop, à droite des boutons -->
          <div class="hidden lg:block w-1/3 pl-12 flex justify-center lg:justify-end">
            <img
              src={ctaIllustration.src}
              alt="illustration contact"
              class="w-full h-auto max-w-sm lg:max-w-full"
              width={400}
              height={400}
              loading="lazy"
            />
          </div>
        </div>
      </div>
    </div>
  </main>
</BaseLayout>

<script>
  document.addEventListener('astro:page-load', () => {
    const appointmentButtons = document.querySelectorAll('[data-cal-link]');
    appointmentButtons.forEach((button: HTMLElement) => {
      button.addEventListener('click', (e) => {
        e.preventDefault();
        // @ts-ignore
        if (typeof Cal !== 'undefined') {
          (window as any).Cal("ui", {
            styles: {branding: {brandColor: "#000000"}},
            hideEventTypeDetails: false,
            layout: "month_view"
          });
        }
      });
    });
  });
</script>